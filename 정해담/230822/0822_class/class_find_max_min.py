

class MaxMin:

    def __init__(self):
        self.find_max_min

    def find_max_min(self, numbers_list):
        max_value = max(numbers_list)
        min_value = min(numbers_list)
        return max_value, min_value
