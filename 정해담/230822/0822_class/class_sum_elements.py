

class SumElements:

    def __init__(self):
        self.sum_elements

    def sum_elements(self, elements):
        for num in elements:
            sum_list = sum(elements)
            return sum_list